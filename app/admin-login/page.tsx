"use client";
import { useState } from "react";
export default function AdminLogin(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=new FormData(e.currentTarget);const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:data.get("email"),password:data.get("password")})});if(response.ok)location.href="/admin";else{setError("Email atau password tidak sesuai.");setBusy(false)}}return <main className="simple-page admin-login-page"><form className="panel admin-login-card" onSubmit={submit}><p className="wordmark">REMPAHKARTA</p><div className="page-title"><p className="eyebrow">Commerce Console</p><h1>Masuk admin</h1><p>Kelola operasional toko melalui akses yang terlindungi.</p></div><div className="field"><label>Email</label><input name="email" type="email" autoComplete="username" required placeholder="admin@rempahkarta.com"/></div><div className="field"><label>Password</label><input name="password" type="password" autoComplete="current-password" required/></div>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button button-dark" disabled={busy}>{busy?"Memeriksa…":"Masuk ke dashboard"}</button></form></main>}
