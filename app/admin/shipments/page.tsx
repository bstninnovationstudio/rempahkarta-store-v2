import { RefreshCw, Truck } from "lucide-react";
import { StatusPill } from "@/components/status-pill";
import { getShipmentRows } from "@/lib/admin-data";
export default async function Shipments(){const rows=await getShipmentRows();return <div className="admin-content"><div className="admin-page-head"><div><p className="eyebrow">Biteship operations</p><h1>Pengiriman</h1><p>Booking, pickup/drop-off, resi, tracking, dan exception.</p></div><button className="button button-light"><RefreshCw size={15}/> Rekonsiliasi</button></div><section className="metrics-grid"><article className="metric-card"><div className="metric-card-head"><span>Total shipment</span><Truck size={15}/></div><strong>{rows.length}</strong><span className="metric-trend">Booking tercatat</span></article><article className="metric-card"><div className="metric-card-head"><span>Menunggu pickup</span></div><strong>{rows.filter(row=>row.status==="handover_pending").length}</strong><span className="metric-trend">Perlu dipantau</span></article><article className="metric-card"><div className="metric-card-head"><span>Dalam perjalanan</span></div><strong>{rows.filter(row=>row.status==="in_transit").length}</strong><span className="metric-trend">Tracking aktif</span></article><article className="metric-card"><div className="metric-card-head"><span>Exception</span></div><strong>0</strong><span className="metric-trend tone-warning">Tinjau webhook</span></article></section><section className="table-card"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Pesanan</th><th>Kurir</th><th>Waybill</th><th>Metode</th><th>Status</th><th>Update terakhir</th><th/></tr></thead><tbody>{rows.map(row=><tr key={row.number}><td><strong>{row.number}</strong></td><td>{row.courier}</td><td>{row.waybill}</td><td>{row.method}</td><td><StatusPill status={row.status}/></td><td>{row.updatedAt}</td><td><strong>Buka →</strong></td></tr>)}{rows.length===0&&<tr><td className="table-empty-state" colSpan={7}>Belum ada pengiriman yang tercatat.</td></tr>}</tbody></table></div></section></div>}
